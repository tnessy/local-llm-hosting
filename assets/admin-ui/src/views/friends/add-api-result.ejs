<%- include("../partials/header", { title: "API friend created" }) %>
<p class="warning">This key will not be shown again — copy it now and send it to <%= username %> out-of-band.</p>
<p><strong><%= username %></strong> (<%= email %>) — added to Authentik + grp-api</p>
<pre id="key"><%= key %></pre>
<button type="button" onclick="navigator.clipboard.writeText(document.getElementById('key').textContent)">Copy</button>
<p><a href="/users">Back to users</a></p>
<%- include("../partials/footer") %>
